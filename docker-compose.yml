version: "3"
services:
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - /data/db
  app:
    container_name: app
    restart: always
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - mongo
    links:
      - mongo
      
  